end2end_eval:
  metrics:
    text_block:
      metric:
        - Edit_dist
    reading_order:
      metric:
        - Edit_dist
  dataset:
    dataset_name: end2end_dataset
    ground_truth:
      data_path: /dataset/OmniDocBench.json
    prediction:
      data_path: /predictions
    match_method: quick_match
