# PR Body Draft — feat(example): real-world RAG ingestion across 5 government and academic PDFs

> Generated by Task 9 (final integration check). Verified 2026-05-20.
> Integration check results: Step 9.1 = 4/5 [ok] (higgs fails, known #260), Step 9.2 = 4/4 tests pass, Step 9.3 = clippy clean.

---

## Summary
- New `rag_realworld` example downloads and chunks 5 real PDFs (ENS, BOE sumario, ATLAS Higgs paper, BSI TR-02102-1, NCSC CAF) into RAG-ready JSONL.
- Offline unit test for the JSONL writer (`tests/rag_realworld_jsonl_test.rs`).
- Weekly GitHub Action `verify-corpus.yml` monitors URL health and opens a tagged issue on failure.

## Why
First concrete showcase aligned with the RAG-first positioning (Causa 2 postmortem). Demonstrates oxidize-pdf ingesting real-world multilingual gov/academic content end-to-end, producing JSONL compatible with LangChain `JSONLoader`, LlamaIndex `JSONReader`, and Pinecone batch upsert.

## Spec
See `docs/superpowers/specs/2026-05-20-rag-realworld-examples-design.md`.

## Integration check results (Task 9 — 2026-05-20)

### Step 9.1 — Clean state run
```
[ok]   ens           → 8279 chunks   ~4 tok/avg   0 oversized   0 headings   out/ens.jsonl
[ok]   boe-sumario   → 1066 chunks   ~24 tok/avg   0 oversized   0 headings   out/boe-sumario.jsonl
[fail] higgs         → parse error: Syntax error at position 169585: Unknown keyword: Sendstream
[ok]   bsi-tr-02102  → 1674 chunks   ~27 tok/avg   12 oversized   211 headings   out/bsi-tr-02102.jsonl
[ok]   ncsc-caf      → 12180 chunks   ~6 tok/avg   0 oversized   0 headings   out/ncsc-caf.jsonl

4/5 documents processed (1 failed) · exit 1
```

Expected ratio: 4/5. The Higgs paper (arXiv:1207.7214) fails with a known parser limitation tracked in issue #260 (stream object /Length mismatch in TeX-generated PDFs). This is intentional skip-and-continue behavior.

### Step 9.2 — Test results
```
test jsonl_line_is_single_line_with_no_internal_newlines ... ok
test jsonl_line_oversized_preserves_flag_and_pages ... ok
test jsonl_line_with_heading_has_all_required_fields ... ok
test jsonl_line_without_heading_serializes_null ... ok

test result: ok. 4 passed; 0 failed; 0 ignored
```

Library tests: `ok. 6388 passed; 0 failed; 3 ignored` — no regression.

### Step 9.3 — Clippy
Clean. No warnings, no fixes required.

## Test plan
- [x] `cargo run --example rag_realworld` produces 4/5 JSONL files in `out/` (1 fails with known issue #260); uses `corpus_cache/` on subsequent runs.
- [x] `cargo test --test rag_realworld_jsonl_test` passes (4 tests, offline).
- [x] `cargo clippy --example rag_realworld -- -D warnings` clean.
- [ ] Post-merge: trigger `verify-corpus` workflow manually from the Actions tab and confirm all 5 URLs return 200.

## Known issue
The Higgs paper (`higgs`) consistently fails with `parse error: Syntax error at position 169585: Unknown keyword: Sendstream`. This is tracked in issue #260 (oxidize-pdf parser does not handle /Length-mismatched stream objects from TeX compilers). The example demonstrates skip-and-continue semantics and exits with code 1 when any document fails — this is correct behavior, not a defect in this PR.

## Follow-up
PRs in `oxidize-python` and `oxidize-dotnet` will mirror this example using the same corpus.
