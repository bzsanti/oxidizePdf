# Release v5.0.0 session handoff — 2026-09-01

## Objective and current status

Carry the v5.0.0 GitFlow release through preparation, validation, merge, tag,
GitHub Release creation, and crates.io publication.

Status at session close: release preparation and merge are complete and the
`v5.0.0` tag is public. The official release workflow is still in progress, so
GitHub Release creation and crates.io publication are not yet verified.

## Release identity

- Release preparation branch: `release/v5.0.0`
- Develop integration merge: `b9cf94a`
- Release metadata commit: `96b9514`
- Release PR: [#563](https://github.com/bzsanti/oxidizePdf/pull/563), merged
  at 2026-09-01T19:29:52Z after all checks passed.
- `main` merge commit: `44ae2babd50b05fd5fa7f9da1d9a8f893259147a`
- Annotated tag: `v5.0.0`, pointing at the `main` merge commit.
- Active release workflow:
  <https://github.com/bzsanti/oxidizePdf/actions/runs/33552849593>

At 2026-09-01T20:20:50Z, `Verify Release Conditions` and `Verify CI Status`
had succeeded. The `Release` job was in progress at `Run tests`; release build,
GitHub Release creation, crates.io publication, and registry availability were
still pending. The local `gh run watch` process was stopped during session
closure; the remote GitHub Actions workflow was not stopped or modified.

## Completed preparation

- Integrated the v4.9.0 release metadata from `main` into the release branch so
  the v5 branch contained the correct published baseline.
- Updated workspace and lockfile versions from 4.9.0 to 5.0.0.
- Updated README dependency guidance to 5.0.0 and its Rust badge to the declared
  MSRV, Rust 1.88.
- Added the v5.0.0 changelog for the policy-driven existing-PDF operations API,
  text rendering modes, invisible/OCR text, `/ActualText`, state restoration,
  fusion boundaries, and the non-exhaustive `TextFragment` change.
- Opened PR #563 against `main`, applied `semver-major`, waited for every check,
  and merged only after CI was green.
- Created and pushed `v5.0.0` from the verified merge commit.

## Validation evidence

Local validation completed before the release commit:

- `cargo fmt --all -- --check`: passed.
- `cargo clippy --workspace -- -D warnings`: passed.
- `cargo test --workspace`: passed, including the T1–T6 corpus suites and 216
  successful documentation tests (26 ignored by design).
- `cargo package -p oxidize-pdf --allow-dirty`: packaged and verified
  `oxidize-pdf v5.0.0` (975 files, 18.7 MiB unpacked, 5.1 MiB compressed).
- `./scripts/verify-examples.sh`: all examples compiled with all features.
- `git diff --check`: passed before commit.
- The pre-commit hook passed formatting, Clippy, and build checks.

PR #563 CI passed examples, MSRV 1.88, SemVer major validation, qpdf, T0/T1,
and stable tests on Ubuntu, macOS, and Windows. The post-merge workflow on
`main` also completed successfully before the release tag workflow began.

## Remaining acceptance criteria and next action

The objective is complete only when both public surfaces are verified:

1. Resume monitoring:
   `gh run watch 33552849593 --exit-status --interval 20`.
2. Require the workflow to finish with `success`, including `Create GitHub
   Release`, `Publish oxidize-pdf to crates.io`, and `Wait for crates.io`.
3. Verify the public GitHub release:
   `gh release view v5.0.0 --json tagName,isDraft,isPrerelease,publishedAt,url`.
4. Verify the registry independently:
   `cargo search oxidize-pdf --limit 1` and confirm version `5.0.0`.
5. If the workflow fails, inspect it with
   `gh run view 33552849593 --log-failed`; do not retag or publish manually
   until the failed step and crates.io state are known.

## Warnings and ownership boundaries

- Packaging warned that locked transitive packages `chacha20 0.10.0` and
  `spin 0.9.8` are yanked. Verification succeeded; handle these separately as
  dependency maintenance rather than changing the release payload.
- Seven earlier handoff reports were untracked before this release closure and
  remain preserved. This new handoff is also intentionally untracked because
  closing the session does not authorize staging, committing, or pushing it.
- The remote release branch still exists at `origin/release/v5.0.0`; branch
  retention or deletion is optional follow-up and was not performed at close.

## Cleanup

- Cleanup started on 2026-09-01; five-day retention cutoff: 2026-08-27.
- Tool: `cargo-sweep 0.8.0` (`cargo-sweep-sweep 0.8.0` version output).
- Exact project-scoped location inspected:
  `/home/santi/repos/BelowZero/oxidizePdf/oxidize-pdf/target`.
- Preview command:
  `cargo sweep --dry-run --time 5 /home/santi/repos/BelowZero/oxidizePdf/oxidize-pdf`.
  It found 881.32 MiB eligible and confined to this workspace's `target`.
- Applied command:
  `cargo sweep --time 5 /home/santi/repos/BelowZero/oxidizePdf/oxidize-pdf`.
  It completed successfully and reclaimed 881.32 MiB.
- No other temporary locations were inspected or cleaned because no other
  narrowly scoped, project-owned disposable location was established.
