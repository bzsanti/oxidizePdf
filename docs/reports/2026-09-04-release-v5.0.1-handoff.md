# Release v5.0.1 session handoff — 2026-09-04

## Objective and current status

Publish `oxidize-pdf 5.0.1` to crates.io through the protected release flow.
Preparation, validation, the PR to `main`, merge, and tagging are complete.
Publication is not yet complete: at the 2026-09-04T15:16:14Z checkpoint the
release workflow was still running its release-mode test step, no GitHub
Release existed yet, and the crates.io publish step had not started.

## Delivery identity

- Release branch: `release/v5.0.1`
- Preparation commit: `4f037cb59a60f784753ae37c34873ec8863aec43`
- Release PR: #571, merged into `main` on 2026-09-04T13:12:24Z
- `main` merge commit: `339129db13874dfcf55ae1292f3e02ac79833283`
- Annotated tag: `v5.0.1`, pointing at that exact `main` merge commit
- Release workflow: <https://github.com/bzsanti/oxidizePdf/actions/runs/33882269396>
- Isolated local clone: `/tmp/oxidize-pdf-release-v5.0.1`

## Release contents

The patch release contains the #564/#570 layout-preserving plaintext quality
correction and the #565/#568 reproducible OmniDocBench gates. The release
commit updates `Cargo.toml`, `Cargo.lock`, `README.md`, and `CHANGELOG.md` from
5.0.0 to 5.0.1. It introduces no public API break and no ISO compliance change.

## Validation evidence

- `cargo fmt --all -- --check`: passed.
- `cargo clippy --workspace -- -D warnings`: passed.
- `cargo test --workspace`: passed with exit code 0; the library portion
  reported 6,777 passed and 3 ignored, and doctests reported 216 passed and 26
  ignored. The #564 regression binary reported 4 passed.
- `cargo package -p oxidize-pdf --allow-dirty`: passed and verified the
  package; 977 files, 18.7 MiB unpacked and 5.1 MiB compressed.
- The packaged manifest contains only target paths (library/examples/tests),
  not path dependencies.
- PR #571 CI passed on Ubuntu, macOS, Windows, MSRV 1.88, SemVer, examples,
  qpdf interoperability, and T0/T1 corpus validation.
- The post-merge `main` runs passed: CI `33876775320`, Corpus Tests
  `33876775386`, Interoperability `33876775352`, and the release-condition
  check `33876775385`.
- The tag-triggered workflow has passed `Verify Release Conditions` and
  `Verify CI Status`; its `Release` job remains in `Run tests`.

Two non-blocking baseline observations were retained:

- The stricter optional command `cargo clippy --workspace --all-targets
  --all-features -- -D warnings` finds two `print_literal` warnings in
  `examples/pdf_to_llm_formats.rs`; both lines date to commit `db0822c9` and
  predate v5.0.0. The release process's required Clippy command passes.
- Packaging warns that lockfile entries `chacha20 0.10.0` (through
  `rand 0.10`) and `spin 0.9.8` are yanked. Package construction and
  verification still pass; no dependency changes were included in this patch.

## Remaining acceptance criteria

1. Confirm workflow `33882269396` finishes with conclusion `success`.
2. Confirm the GitHub Release `v5.0.1` exists and is neither draft nor
   prerelease.
3. Confirm crates.io exposes `oxidize-pdf` version `5.0.1`.
4. Only after all three checks pass, consider the release objective complete.

Suggested point-in-time commands (do not use a long-running watcher):

```bash
gh run view 33882269396 --json status,conclusion,url,jobs
gh release view v5.0.1 --json tagName,isDraft,isPrerelease,publishedAt,url
cargo search oxidize-pdf --limit 1
```

## Ownership boundaries and cleanup

The primary workspace remained on
`fix/issue-564-omnidocbench-text-quality` at
`b10409c8d5712223122726279c26de644c27d817`. Pre-existing or user-owned
changes in `README.md`,
`docs/reports/2026-09-02-issue-565-omnidocbench-reading-order.md`,
`oxidize-pdf-core/Cargo.toml`, and eight untracked handoff reports dated
2026-08-25 through 2026-09-01 were not staged or modified.

Cleanup used `cargo-sweep 0.8.0` with the five-day cutoff
2026-08-30T15:16:14Z. The dry run and actual run against the primary workspace
removed 2.79 GiB of eligible build artifacts from its `target`. The isolated
release clone was inventoried at 8,423,243,778 bytes and 37,851 files, with no
symbolic links; it was created on 2026-09-04 and is still release evidence, so
it was preserved. Both dry-run and actual `cargo sweep --time 5` against its
`target` reported nothing eligible. No other temporary paths were deleted.
